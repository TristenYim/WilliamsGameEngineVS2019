#pragma once

// TODO: Make this
// A currently known bug is that if projectiles are moving too quickly, they won't collide with the offense bots and therefor won't actually kill them
// Currently my best idea is to spawn a special kind of object at a regular interval in the path of an object to ensure it will hit the right target if it can
// However this is tedious and changes little